# 163 自动进入日常 AI 索引：真实邮件验收

验收日期：2026-09-14。结论：功能已实现并在当前机器启用；当前账号全部可选文件夹的38封真实邮件已进入日常正文索引。真实定时任务、逐封问答、普通聊天和 Agent 入口均已完成最终验收，Astra 做了独立代码与语义复核。以下保留失败记录和测试边界，不表示任意邮件、任意问题均不会出错。

## 已交付行为

- 手动同步与后台计划共用自动索引流程，已下载邮件也会补入；本机 `.env` 已设置 `MAIL_AI_INDEX_ENABLED=true`，项目默认值仍为关闭。
- 以该账号本地已扫描、仍存在、解析成功且未标记删除的邮件更新正文索引。保留其他导入数据及同一 owner 的其他邮箱资料，发布失败或取消时保留上一代，后续同步重试；这些故障、删除和并发行为使用隔离回归测试验证。
- 无变化时复用向量和索引代；邮箱界面展示索引数量、状态与失败信息。附件仅保留元数据及解析覆盖信息，提取文本不单独进入正文向量索引。
- 已保留启动本机 API `127.0.0.1:8000` 和前端 `127.0.0.1:8501`。现有计划覆盖当前8个可选文件夹，间隔300秒、每次同步上限沿用100封。API 运行期间自动处理；未配置开机自启，停止 API 后计划暂停执行。未来新增文件夹需加入同步计划。

## 本次实际结果

| 项目 | 结果与证据含义 |
|---|---|
| 邮箱覆盖 | 1个163账号，8个当前可选文件夹，共38封；解析失败0、未下载0 |
| 原件与正文 | 38/38原始 EML 哈希一致；38/38分页回读正文与标准清洗结果完整一致；38/38本地关键词查找成功 |
| 正文入索引 | 新增38封、328个片段；原有5000封导入资料、5001个片段保留。索引总计5038封、5329片段，不能把5038封都当成真实邮箱邮件 |
| 保留与幂等 | 原5001片段正文及元数据逐条一致，仅允许索引代字段改变；重复同步下载0、新嵌入0，索引代不变 |
| 真实问答 | 每封一题，共38题；HTTP、完成状态、目标召回、目标引用、引用身份、正文连续原句自动检查均38/38通过；58/58正式引用身份有效 |
| Astra语义复核 | 逐题查看回答、原文与来源，摘要和连续原句38/38通过；另有3项非阻断展示问题，见下文 |
| 聊天入口 | 最新代码的 `/chat`、`/chat/agent`、`/chat/graph` 邮箱状态查询3/3通过，不调用远端聊天模型；普通 `/chat` 内容问答及 `/jobs/agent` 内容任务2/2通过，引用有效，Astra核对语义通过 |
| 日常定时任务 | 启用计划后，由实际后台调度产生的新任务成功；覆盖8文件夹，索引 ready、38封/328片段，再次下载0、嵌入0，与已测索引代一致 |
| 全量回归 | 最终 Windows `1526 passed, 3 skipped, 190 subtests passed`，退出码0；测试期间源码不变 |
| 远端状态 | 全量同步前后 UID 与 FLAGS 一致；同步及日常任务命令审计未出现写邮件操作，使用只读选择和 PEEK 抓取 |

3项跳过：2项需要当前 Windows 账号未具备的符号链接创建能力；1项仅检查 Linux 目录描述符审计事件。没有将跳过记成通过。本轮未重新执行 Linux / Docker 验收，旧报告只能作为历史结果。

真实问答沿用本机配置的 DeepSeek、查询改写、向量/BM25/RRF及 crossencoder 重排。索引嵌入在本地计算，入索引阶段聊天模型调用数为0；问答实际向已配置模型提交相关真实邮件片段。模型费用缺少定价信息，不把记录中的未计价金额0解释为免费。双并发问答与回归有重叠，本次不是独立性能压测。

## 实测发现并修复的问题

| 严重程度 | 发现 | 为什么构成问题、可能后果 | 最小修复与验证 |
|---|---|---|---|
| P2 中 | 仅含主题的首片段占据检索结果 | 正文存在却被判断为空，无法给出实际摘要 | 在同一索引快照有界补读相邻正文，校验来源与位置；全38题及回归验证 |
| P2 中 | UTC日期与本地日期比较不一致 | 跨日邮件被错误判定为不符合用户日期，普通问答和Agent均实际出现过 | 普通生成与摘要显示按检索时区换算的日期；Agent补充相同日期语义，保留原始元数据；跨时区回归与真实入口复测 |
| P2 中 | 纯空白分隔片段进入重排 | 相关邮件只召回空白，回答无法获得正文 | 重排前排除纯空白候选，保留非空表格行；问题邮件定向及38题验证 |
| P2 中 | 用省略号拼接不连续内容作为原句 | 用户要求的逐字证据被改写，引用不能证明所展示的连续原句 | 明确连续原文及跨片段引用规则；问题题目与38题复测 |
| P2 中 | Agent仅输出片段ID，缺少完整引用格式 | 引用无法进入既有邮件/片段身份校验流程 | 仅对本次实际可见且归属唯一的片段补全，仍执行版本和身份校验；歧义、候选、Markdown链接边界回归及真实任务验证 |

Agent接口附加验收中，两次旧提示任务均触及既有步骤上限，记录为 `incomplete`；首次还错误否定本地日期，并误称全文已读。游标复核表明，工具输出预算裁剪后正确要求续读；未修改游标实现或扩大预算。补齐日期规则，并明确单句摘要有充分证据即可停止、不得宣称未读全文已读后，同一问题的新会话任务成功，5次工具调用，正确说明630字符仅读至378且仍有未读尾部；原句落在实际可见范围内。一次成功不能证明所有Agent任务都能在预算内完成。

## 失败记录与修复后结果

- 最早不带日期的诊断运行完成6题，自动通过3题，发现主题片段问题后中止。后续将实际本地发件日期写入题目，区分同主题邮件；保留原始诊断题与结果。
- 下一轮完成9题，自动通过7题，但Astra额外发现4题日期语义错误。自动检查没有覆盖这些语义错误，没有将自动通过等同于语义通过。
- 随后一轮完整38题，自动及严格语义通过36题，暴露空白召回与非连续原句问题。修复后最终38题全部通过，原失败轮完整保留。
- Agent附加接口两次旧提示任务未完成；修复后的最新普通聊天和Agent任务通过。未删除旧结果或放宽成功状态断言。
- 早期全量回归遇到 Windows 子进程启动竞态与验收临时路径过长：前者在测试中增加启动门控，保留互斥断言；后者缩短项目外测试路径，未为此修改业务逻辑。
- Agent系统提示更新后，一项历史裁剪测试仍按旧静态提示计算预算，导致一轮 `1525 passed, 1 failed`。已改用实际动态提示计算同一测试基准，保留首轮完整历史及后续逐出历史的断言；最终全套通过。

## 仍需明确的边界

- 当前38封均核对了完整清洗后正文，但每封只执行一个带实际日期和主题的摘要/原句问题，不能外推复杂跨信推理、所有问题类型或未来邮件。
- 两封邮件包含3个日历 MIME 附件部件，状态为2 complete、1 partial。3个部件的原始解码字节与提取文本逐一核对一致；partial部件未声明字符集，与另一声明类型的日历部件字节相同，保留保守警告。未据此声称附件全文 AI 检索已完成。
- Astra记录3项非阻断展示问题：M01把同一邮件的两个片段称作“邮件1/2”；M04补充说明有来源支持但未单独标注对应片段；M19摘录了非必要订单/文稿编号。没有修改题目或删除结果来消除这些记录。
- 继续采用单 owner、单 API 实例、单 worker。AI查询当前 owner 的共享索引，邮箱选择不自动限制AI检索范围。关闭开关不会清除已入索引资料；旧索引代继续按既有留存策略保留。
- 已验收一次真实调度运行及重复同步，不等同于长期无人值守稳定性测试。没有向外发送邮件，也没有通过删除远端真实邮件验证删除传播；删除传播由隔离回归验证。

## 可核对的证据

私人邮件、账号数据、查询与模型原始响应保存在项目外 `E:/email-agent-validation/auto-index-20260914/`，本报告不复制邮件正文、主题、私人地址或凭据。同步前已备份日常索引、邮件数据库、账号和计划数据库。本轮没有新增下载安装。

- `live-sync.json`、`index-preservation.json`：全量同步、重复同步、38封原件/正文/搜索检查及旧资料保留。
- `rag-results.private.json`、`rag-frozen-inputs.json`、`astra-semantic-review.private.json`：最终38题原始结果、冻结输入及逐题独立语义结论。
- `chat-first-incomplete.private.json`、`chat-second-incomplete.private.json`、`chat-acceptance.private.json`：两次未完成接口任务及修复后接口验收。
- `daily-activation.private.json`、`daily-config-change.json`、`daily-imap-audit.json`：日常计划执行、仅变更自动索引开关的配置核对和只读命令记录。
- `reg-release/pytest.log`、`reg-release/junit.xml`、`reg-release/result.json`：最终全量回归；此前运行分别保留原目录。
- `final-checks.json`、`final-source-manifest.json`、`rag-scope-proof.json`、`astra-acceptance.md`：最终一致性核对、逐文件源码摘要与Astra复核。

最终业务源码SHA-256：`dc39c61b07e0d02e07503f18965d3f6441fe6f87c7fa30824202e0e20f616963`。最终38题 `/query` 执行时源码SHA-256：`bf394c2a95bcf2bddd656cf14043aac2ad9bdcb622f1d8a39234887ed6b21448`。两者之间仅 `agents/agent_loop.py` 的系统提示变化；在内存逆转这几处提示变更后重算全部业务文件哈希，准确匹配38题冻结摘要。检索/生成/索引路径未变，Agent另用最终源码实测，全量回归也基于最终源码；没有把不同源码摘要说成同一轮执行。
