# Astra 独立交叉审核

审核日期：2026-09-14（北京时间）。审核者：Astra 子 agent。结论：**本轮三项收尾工作可验收，未发现需要阻塞交付的缺陷。** 结论限于下述代码和证据范围，不表示项目全面无漏洞。

## 审核范围与方法

按 `C:/Users/14868/.codex/skills/code-review/SKILL.md` 独立审阅 README 当前状态及历史标识、固定合成评测的数据/脚本/测试、来源回读控件 key 修复和本目录验收证据。仓库中先前已有的其他未提交修改不视为本轮新增问题；源码指纹仍按完整当前工作区核对。

仅进行代码及本地证据只读检查、离线测试、视频解码和 SQLite 只读查询。没有重新调用真实模型或真实邮箱，没有修改业务代码、测试或评分规则。

## 独立检查结果

| 项目 | 独立检查及结果 |
|---|---|
| 文档与链接 | 阅读项目 README、本目录 README/evaluation/demo 及历史评测顶部说明。当前实测、前一轮 Linux/Docker 基线与 V1–V7 历史成绩已分开；模拟审批、163 未自动接入 AI、单 owner/worker 和未覆盖范围均有明确说明。检查这些文档的本地链接，除本报告生成前的自引用外均存在；本报告补齐该入口。未联网验证外部网页。 |
| 隔离与真实调用路径 | 脚本要求仓库外全新目录，拒绝复用；隔离索引、账号、审批、会话和任务路径，禁用 dotenv 自动加载并只继承模型/缓存配置白名单，固定 simulated provider 并关闭邮箱调度。检查实际 `/query` 路由调用 retrieve/generate_answer，脚本未发送 gold 或替换模型答案。 |
| 固定数据与失败口径 | 12 封邮件及 12 题 ID 唯一、gold 有来源；每题一次 HTTP 请求。HTTP/请求解析失败按失败题保留，缺失 usage 不当作零总量；无脚本级重试覆盖答案。启动或索引失败会中止，不能当作一份已完成的 12 题评测。 |
| 评分及用量 | 使用当前脚本逐题重算 score，与归档完全一致；重算 aggregate 一致。检查逐请求 provider calls 的 token 和等于 actual_total_tokens，usage_source 均为 provider。结果为 12/12、gold 邮件平均 recall@5=1、14/14 引用 ID 有效、15413 tokens；均值 1.8451419 秒，最近秩 p95=2.8592562 秒。 |
| 版本一致性 | 重新生成源码 manifest，与归档文件完全一致；最终评测、demo-workflow 和当前工作区 SHA256 均为 `bac134dde6211fae71fe39a1e9500f9f5fc1873763cface35e30baeac9edeb6e`。指纹覆盖源码/依赖清单/数据/评分脚本，不包含文档或秘密配置；它不是远端模型权重证明。 |
| UI 修复 | 当前回答渲染时已先追加 user 消息，`len(messages)` 正好是随后追加 assistant 的历史下标；下一轮 enumerate 使用相同 key。已有历史消息下标小于新下标，不引入同次渲染重复 key。具体来源回读状态还包含 ref 哈希，未发现本次修改导致不同来源复用错误。 |
| 独立离线回归 | 设置 PYTHONUTF8=1 和 PYTHONIOENCODING=utf-8，执行 `.venv/Scripts/python.exe scripts/offline_tests.py tests/test_portfolio_eval.py tests/test_ctx_frontend.py -q`：**14 passed in 3.30s**。包含聊天首页及高级工作台首击回读、错误引用、未完成回答、失败分母、未知 usage 和目录隔离场景。 |
| 真实演示与审批 | 阅读 browser-events、demo-workflow 与截图文字；查看原文回读及审批后截图。以 SQLite `mode=ro` 打开 demo-03 的 jobs/approvals 数据库，归档任务的 kind/status/request/result 和审批的 status/execution_state/payload/result 均与真实记录相同。审批为 approved/succeeded、provider=simulated、sent=false，未声称真实发送。 |
| 视频 | 独立 ffprobe 确认 H.264、1440×1064、56.52 秒、2617672 字节；ffmpeg 全片解码至 null 成功，无解码错误；视频 SHA256 与 video-verification.json 一致。截图支持来源回读和模拟审批展示。本审核没有逐帧人工审看全片，也不把哈希或无 pageerror 当作全面 UX 验证。 |
| 全量测试证据 | 阅读最终 Windows 输出，确认 1490 passed、3 skipped、190 subtests、187.84 秒；该数字来自主 agent 保存的执行证据，本审核未重跑全量或 Linux/Docker。首次编码失败与最终通过分开保留，文档没有将旧跨平台结果冒充本轮重跑。 |

## 逐题事实与引用独立核对

直接阅读最终 evaluation-results.json 的回答及其来源列表，并对照完整合成邮件，不仅依据自动 passed 字段。

| 题目 | Astra 逐题核对结论 |
|---|---|
| Q01 | 正式预算 12800 元及替代 9600 元初稿的说明，均由正式预算邮件支持。 |
| Q02 | 林测试是星舟负责人，引用正确。 |
| Q03 | 2026-09-18 上午 10 点、梧桐会议室，与会议邮件一致。 |
| Q04 | 9600 元初稿、12800 元正式确认及以后者为准，引用两封正确邮件。 |
| Q05 | 9 月 25 日前、PDF、文件名要求和不得用截图替代，均有原文依据。 |
| Q06 | 星舟 12800 元、海岚 8600 元，两份引用正确；差额 4200 元计算正确。 |
| Q07 | 86 个通过用例、2 个未修复阻断缺陷及全部关闭前不可发布，未把测试通过等同于允许发布。 |
| Q08 | 工作日 09:00–17:00、许示例、周末及紧急事项限制均与北辰原文一致。 |
| Q09 | 英文回答中的 24 sensor units、2026-09-22、Warehouse B 均对应 Orion 邮件。 |
| Q10 | 海岚银杏会议室及附带日期时间正确，没有混入星舟地点。 |
| Q11 | 明确未提供线上链接，与邮件明示内容一致，没有编造 URL。 |
| Q12 | 明确正文无金额、附件未解析，无法确定违约金；未用无关预算猜测。 |

12 题未发现事实错误或引用与陈述不相符。14 次引用均可在实际返回来源中定位。

## 问题与验收边界

- **确定缺陷：未发现本轮范围内的阻塞问题。** 不提出纯风格修改作为验收条件。
- **评测限制：** regex 只能核对预设词句，不能完整识别否定、对象错配或额外幻觉；引用 ID 有效也不证明语义支持。本次独立逐题审阅补充了这 12 题的事实检查，不能外推真实邮箱质量。现有报告已正确说明这些边界。
- **覆盖限制：** 12 题为简短、明确项目名称的固定合成样本；信息不足题在邮件和问题中有明确提示，不是广泛抗幻觉或提示注入测试。Agent 录像明确指定了工具顺序，只证明该流程和模拟人工审批可运行。
- **证据限制：** 只读检查证明归档和当前本地记录一致，不是对历史录制过程的独立现场见证；未验证真实发信、Gmail/163 在线能力、长期运行、并发、托管 CI 或依赖漏洞库。

在上述范围内，文档陈述、当前源码、评测响应、离线回归与演示审批证据相互一致，支持本轮验收。
